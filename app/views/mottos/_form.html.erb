<%= form_for(@motto) do |f| %>
  <% if @motto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@motto.errors.count, "error") %> prohibited this motto from being saved:</h2>

      <ul>
      <% @motto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :downloaded_at %><br>
    <%= f.text_field :downloaded_at %>
  </div>
  <div class="field">
    <%= f.label :remote_image_url %><br>
    <%= f.text_field :remote_image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
